{
  "name": "amazon-wishlist-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply wishlist-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply wishlist-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
